version: '3.8'

services:
  app:
    build: .
    ports:
      - "8088:8088"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - APP_ENV=development
      - LOG_LEVEL=DEBUG
      - DATABASE_TYPE=chroma
    depends_on:
      - chroma
    command: python main.py

  chroma:
    image: chromadb/chroma:latest
    ports:
      - "8088:8088"
    volumes:
      - ./chroma_db:/data  # Mount to Chroma's default data directory
    environment:
      - ALLOW_RESET=true
      - CHROMA_API_IMPL=rest
      - CHROMA_SERVER_HOST=chroma
      - CHROMA_SERVER_HTTP_PORT=8088

volumes:
  chroma-data:
